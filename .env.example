# Prometheus HTTP API base URL from your benchmark / other repo (no trailing slash).
# Examples: http://127.0.0.1:9090   http://benchmark-host:9090
PROMETHEUS_URL=http://127.0.0.1:9090

# Port for this app (prom-web / uvicorn). Change if 8001 conflicts with another service.
WEB_PORT=7777
